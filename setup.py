import setuptools

setuptools.setup(name='buzzsprout_uploader',
      packages=setuptools.find_packages(),
      install_requires=['bsm'],
      version='1.0',
      description='Upload a podcast to Buzzsprout',
      author='CodingVibe',
      url='https://github.com/codingvibe/buzzsprout-uploader'
     )